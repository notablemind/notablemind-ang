
server {
    listen       notablemind.local:80;
    server_name  notablemind.local;

    include root.conf;
    try_files $uri @node;

    location @node {
        proxy_pass http://localhost:3000;
    }
}

